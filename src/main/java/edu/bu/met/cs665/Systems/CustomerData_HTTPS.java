package edu.bu.met.cs665.Systems;

public interface CustomerData_HTTPS {
    // Declaration of the printCustomer method, which takes a customerId as a parameter
    void printCustomer(int customerId);

    // Declaration of the getCustomer_HTTPS method, which takes a customerId as a parameter
    void getCustomer_HTTPS(int customerId);
}
